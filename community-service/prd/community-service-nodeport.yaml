apiVersion: v1
kind: Service
metadata:
  name: community-service-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app: community-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30090
